[project]
name = "fs-save-editor"
version = "0.1.0"
description = "Flexible Survival Save Editor"
requires-python = ">=3.8"
dependencies = [
    "pyside6",
    "darkdetect",
]

[tool.uv]
package = false
